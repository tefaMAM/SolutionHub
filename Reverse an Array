class Solution {
    public void reverseArray(int arr[]) {
        if(arr.length==1){return;}
        int j=arr.length-1;
        int i=0;
        while (i<j){
            int temp=arr[j];
            arr[j]=arr[i];
            arr[i]=temp;
            i++;
            j--;
        }
    }
}
